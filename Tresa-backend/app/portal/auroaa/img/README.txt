Place your background image here as: bg.jpg
Recommended size: 1920x1080 or larger, landscape.
All portal pages use img/bg.jpg in style.css (with an online fallback if the file is missing).

Icons are embedded inline in each HTML page (not icons.svg) so they work on the router and when opening files locally.
Optional: icons-sprite.html is a reference copy of the symbol definitions.
